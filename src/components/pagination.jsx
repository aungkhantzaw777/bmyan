import { Button, Form, Table, Pagination } from 'react-bootstrap';

function pagination(prop) {

    const { last_page } = prop

    var items = () => {
        return (
            <pagination.items>2</pagination.items>
        )
    }

    
    return (
        <>

        </>
    )
}

export default pagination